# Financial Analyzer 改进方案

> 制定日：2026-05-26
> 目的：将这个有価証券報告書分析工具打磨成可在求職場面展示的、能体现大学院研究 / 数据分析 / 工程能力的作品。

---

## 一、现状诊断（現状分析）

### 1.1 技术栈现状

| 层 | 技术 | 评价 |
|----|------|------|
| 后端 | FastAPI + uvicorn | 干净，模块拆分合理（auth / pdf_parser / analysis / reporting / storage） |
| PDF 解析 | PyMuPDF + 正则关键词匹配 | 规则硬编码，对不同企业模板鲁棒性低 |
| 前端 | Jinja2 + 原生 JS + 手写 CSS | 无框架，SVG 图表自绘，CSS 单文件 1700 行规模 |
| 存储 | JSON 文件落地 | 适合个人/小规模，无并发问题 |
| 图表 | 手绘 SVG（line / bar / scatter） | 灵活但缺少交互（zoom / pan / tooltip 不规范） |
| 认证 | 自实现 signed cookie + PBKDF2 | OK |
| 部署 | systemd + Caddy + symlink releases | 已经很规范 |

### 1.2 用户提出的核心问题

1. **UI 难看**：色调单一（绿白灰），缺少层次，信息密度低；像内部工具，不像产品。
2. **操作逻辑模糊**：模式切换、过滤、PDF 选择、报告生成的关系不直观；新用户上手要试错。
3. **解析鲁棒性有限**：关键词命中即取，遇到同义词、合并行、变体表头会失败；用户看不到"为什么没抽到"。
4. **其他**（我的观察）：
   - **没有 Demo / 样例数据**：访客必须自带 PDF，否则首页 → 上传页 → 空分析页，无法体验产品价值。**这对求職展示是致命的**。
   - **没有数据导出**：财务分析师天然期待 CSV / Excel 导出，目前只有 HTML 报告。
   - **抽出信頼度只有一个数字**：用户不知道具体哪个指标没抽到、为什么。
   - **PDF 原文与抽出值无视觉关联**：无法点击数字回到 PDF 对应页/位置验证。
   - **移动端基本不可用**：sidebar 固定宽度，表格横向溢出。
   - **缺少"作品介绍"叙事**：没有 About 页、没有技术博客链接、没有 GitHub 链接，HR/技術面試官 看不到背后的思考。
   - **首页 feature 文案干瘪**：4 个 box 都是说明性文字，没有视觉示例（截图 / 动图 / 数据）。
   - **登录墙挡在最外层**：HR 想"5 秒内 understand 这个产品"时会被劝退。

### 1.3 作为「求職作品」的视角

求職展示的核心矛盾是：**HR / 面試官 通常只看 30 秒～3 分钟，必须在这个时间内传达"这个人能做什么、思考深度如何、技术栈宽广度如何"**。

当前产品在这点上几乎为零：
- 进入 fin.zekkx.icu 看到的是登录页。
- 即使登录，没有 PDF 就看不到任何东西。
- 没有任何文字说明"这是大学院研究 + 自主开发的项目，技术亮点是 X / Y / Z"。

---

## 二、改进愿景（あるべき姿）

> **从「能用的内部工具」改造为「能讲故事的展示作品」**

具体形象：
- 访客打开 `fin.zekkx.icu` → 即看到一张精心设计的 landing 页，说明产品定位、技术亮点、3 张内置 demo 入口。
- 点击 demo → 直接看到完整的多公司比較 / 时系列分析 / カスタム分析 结果，UI 是仪表盘级别。
- 自己上传 PDF 也支持，但是是"次要入口"（登录后），不是访客必经路径。
- About 页 / Tech 页讲清楚：解析架构、鲁棒性策略、可视化设计原则、未来计划。
- 整个体验 30 秒内能让人产生「この人、まともにモノづくりができる」的判断。

---

## 三、改进方向（具体的な改善案）

按"对求职价值的贡献度 × 实施复杂度"排序，分 5 大方向。

### 方向 A：访客入口与作品叙事（最高优先级 / 求职杀招）

**A1. Landing 页**（全新）
- 公开访问，不需要登录。
- 内容：
  - 顶部 Hero：产品名 + 一句话定位（例如「有価証券報告書 を読み解く、研究者のための分析ダッシュボード」）。
  - 内置 Demo 入口：3 张卡片 → トヨタ・ソニー・任天堂 5 年推移 / 同業界 4 社同年度比較 / 自由カスタム例。
  - 技术亮点小区：PDF 構造解析 / 13 指標自動抽出 / 派生 12 指標自動計算 / etc。
  - 末尾：作者紹介、GitHub、ログイン入口。
- 视觉风格：参考 [Linear](https://linear.app) / [Vercel](https://vercel.com) / [スマートHR](https://smarthr.jp) 的现代 SaaS 着陆页。

**A2. Demo 数据集**
- 在服务器准备 3～5 个高质量的预制分析会话，作为"展示用 sample analyses"。
- 访客访问 demo 入口 → 直接进入 `analysis_id=demo-xxxxx` 的分析页（只读，不能上传/删除/补正）。
- 数据来自实际上市公司的有報。

**A3. About / Tech 页**
- `/about`：作者介绍、研究主题、与本工具的关系。
- `/tech`：技术架构图（FastAPI / PyMuPDF / 自製 SVG charts / Caddy）、解析策略说明、设计哲学。
- 这里直接体现「大学院研究成果」与「コード力」。

---

### 方向 B：UI / UX 全面重做（次高优先级 / 直接解决用户 1+2 问题）

**B1. デザインシステム再構築**
- 当前色板单调（一种绿）。引入：
  - **主色**：深邃的 navy（金融感）或 deep teal（信頼感）
  - **強調色**：温暖的 amber（数据高亮）
  - **失败/警告色**：抑制的赤
  - **中性色阶**：8 段灰，提升层次
- 字体：日文 `Noto Sans JP` + 等宽数字 `JetBrains Mono` / `IBM Plex Mono`（金額の桁揃え）。
- 间距 / 阴影 / 圆角统一令牌化（已有 `:root` 变量，需要扩张和重新平衡）。

**B2. レイアウト再設計**
- 当前是 sidebar + main，sidebar 信息过密（mode 切换、フィルター、PDF 列表、フッターボタン都在一起）。
- 提议：
  - **トップバー**：固定，含 logo / 当前模式 / 全局动作（导出・レポート・设定）
  - **左ナビ**：仅放「概要 / 指標 / チャート / PDF管理」4 个 tab（替代当前 tab-nav）
  - **メインエリア**：内容专注，过滤条件放在内容头部的 toolbar
  - 移除 sidebar 中的 PDF 列表（放到「PDF管理」tab）

**B3. 操作流程明确化**
- 当前模式切换通过 segmented control，但模式切换会改变过滤区可见项，新用户难理解。
- 提议：
  - 进入分析页 → 顶部一行明确"现在你在看什么"：例如「同一企業（トヨタ）の 2020–2024 年推移」
  - 旁边一个「分析を変更」按钮 → 弹出 modal 引导式重新配置（步骤化：1. 选模式 → 2. 选目标 → 3. 选图表类型）
  - 当前 sidebar 形式的"什么都堆在一起"改为"明确目标 + 想改时再开"

**B4. チャート操作性**
- 当前 SVG 图表是静态的，hover tooltip 只有 CSS pseudo。
- 提议：
  - 引入 [Chart.js](https://www.chartjs.org/) 或 [Apache ECharts](https://echarts.apache.org/) 替代手写 SVG。
  - ECharts 对中日韩有官方支持，文档良好。
  - 获得：缩放、平移、tooltip、图例切换、保存为图片等"金融分析师习惯的"操作。
  - 缺点：增加前端依赖（但只是 CDN 一行）。

**B5. 数据密度提升**
- 当前 KPI 卡片只显示数值，缺少趋势箭头、迷你 sparkline。
- 提议：每个 KPI 加 mini sparkline（show 历史推移）、上下三角箭头（vs 前年）。

---

### 方向 C：PDF 解析鲁棒性升级（直接解决问题 3 / 体现技术深度）

**C1. 多策略融合**
当前策略只有"关键词匹配 + 取后续数字"。提议增加：

| 策略 | 实现 | 应对场景 |
|------|------|---------|
| 关键词命中 | 现有 | 标准表头 |
| 表格结构识别 | PyMuPDF `page.find_tables()` | 表格中数字位置定位 |
| 同义词词典 | 手工维护，例如「売上高 ⇔ 営業収益 ⇔ 売上収益」 | 业种间表述差异 |
| 数字-标签位置关联 | bbox 坐标距离 | 关键词与数字跨行 |
| LLM 后备（可选） | 抽出失败时调用 DeepSeek / GPT 提取 | 极端情况 |

**C2. 抽出诊断 UI**
- 当前用户看到「信頼度 60%」但不知道哪 40% 失败。
- 提议：每个指标在表格里显示
  - ✓ 自動抽出（绿色 dot + 来源 page）
  - ⚠ 同義語マッチ（黄色 dot + 使用的别名）
  - ✗ 抽出失敗（灰色 + 「補正する」按钮）
- 点击指标 → 弹出「該当 PDF ページ + 高亮位置」（用 PyMuPDF 渲染该 page 为 image）

**C3. PDF 原文ビューア**
- 「指標値をクリック → 對應 PDF ページが右側に表示され、該当行がハイライト」
- 这是体现"严肃做了 PDF 解析"的最直观方式。
- 技术实现：服务器把该 page 渲染为 PNG/SVG，标记 bbox，前端 modal 显示。

---

### 方向 D：产品完整性（拉满成熟度）

**D1. 数据导出**
- CSV：表格内容 1:1 导出。
- Excel：用 `openpyxl`，多 sheet（原始指标 / 派生指标 / 推移）。
- JSON：完整分析结果（給 API ユーザー / 給データ サイエンティスト）。

**D2. 分享リンク**
- 现在 `/analysis/{id}` 是临时（7 日清理）。
- 提议：admin 可以「永続化 + 共有」一个分析作为公开 demo（专用表 / flag）。

**D3. 印刷品質レポート**
- 现在 HTML 报告是 standalone。改进：
  - A4 印刷 CSS（`@page` rules、避页眉切断表格）。
  - PDF 出力（用 `weasyprint` 或 `playwright`）。
- 这能直接出现在简历里：「実務で配布できる品質のレポート出力対応」。

**D4. 多言語**
- 当前完全日文。考虑「日 / 中 / 英」切换。
- 国際企業（外資系 / 海外採用）面試官能用英文看，扩大潜在评价人群。

**D5. レスポンシブ**
- 当前 sidebar 固定 280px，table 横向溢出。
- 提议：< 768px → sidebar 变 drawer，表格变卡片堆叠（每行一个 row card）。
- HR 用手机点开看 demo 是常見シーン。

---

### 方向 E：工程品質（体现コーディング力）

**E1. 测试**
- 当前 0 测试。提议：
  - `tests/test_pdf_parser.py`：对 fixture PDF 测各指标抽出。
  - `tests/test_analysis.py`：派生指标计算。
  - `tests/test_api.py`：FastAPI TestClient（认证 / 上传 / 分析）。
- 加 GitHub Actions（即使是私库也行，体现 CI 意识）。

**E2. 型号 hint 完善 + ruff/mypy**
- 当前大部分有 type hint，但没工具检查。
- 加 `pyproject.toml` + `ruff` + `mypy strict` + pre-commit。
- 这些都是日本企业研究开発 / バックエンド 招聘看重的「品質意識」。

**E3. 観測性**
- structlog で構造化ログ。
- `/metrics` Prometheus endpoint（即使没人 scrape）。
- 错误时 Sentry（可选）。

**E4. README 重写**
- 当前 README 是「運維説明書」面向操作者。
- 加一个 `README_for_recruiters.md`：项目背景 / 技术亮点 / 解析架构图 / 关键代码段引用 / 自我评価。

---

## 四、实施阶段提案（フェーズ分け）

按 **「最小投资 → 最大求職効果」** 排序：

### Phase 1：作品化基础（约 2～3 个工作日）
- ✅ A1 Landing 页
- ✅ A2 Demo 数据集（手动准备 3 个 sample）
- ✅ A3 About / Tech 页骨架
- ✅ B1 デザインシステム（色板 + 字体 + 间距重整）

→ 完成此 phase 即可投到 ES（エントリーシート）链接，已经能看了。

### Phase 2：UI / UX 深化（约 3～4 个工作日）
- ✅ B2 レイアウト再設計
- ✅ B3 操作流程明確化（modal 引导）
- ✅ B4 ECharts 引入
- ✅ B5 KPI sparkline
- ✅ D5 レスポンシブ

→ 此 phase 完成后，HR 在 PC 和手机上看都不会觉得"内部工具感"。

### Phase 3：技术深度展示（约 4～6 个工作日）
- ✅ C1 多策略 PDF 解析
- ✅ C2 抽出診断 UI
- ✅ C3 PDF 原文ビューア
- ✅ D1 データ エクスポート
- ✅ D3 印刷品質レポート

→ 此 phase 是「技術面試」时给面試官 demo 的核心，体现深度。

### Phase 4：工程品質（约 2～3 个工作日）
- ✅ E1 测试
- ✅ E2 ruff/mypy/pre-commit
- ✅ E4 README_for_recruiters

→ 此 phase 让面試官查 GitHub 时留下良い印象。

### Phase 5（オプション）
- D2 分享リンク
- D4 多言語
- C1 中的 LLM 后备

---

## 五、要讨论决定的事项

请你回复以下问题，我再据此细化和动手：

1. **方向取舍**：A～E 五个方向你觉得哪些必须做？哪些可以舍弃？
2. **UI 風格偏好**：
   - (a) Linear / Vercel 风（极简 / 黑白灰 / 强调留白）
   - (b) Bloomberg Terminal 风（暗色 / 高密度 / 等宽字体 / 数据感）
   - (c) freee / マネーフォワード 风（清爽 / 蓝白 / 日本企業に親しみ）
   - (d) 其他你想到的参考
3. **是否允许引入前端框架**：
   - 维持现状（Jinja + vanilla JS）：部署简单，但复杂交互难
   - 引入 React/Vue + Vite：能做更精致的 UI，但部署链增加
   - 折衷：保留 Jinja，仅在需要的地方用 [Alpine.js](https://alpinejs.dev/) 或 [htmx](https://htmx.org/) 增强（推荐）
4. **是否引入 LLM**：方向 C 中的 LLM 后备会触达隐私 / 成本问题，要不要保留这个选项？
5. **Demo 数据的来源**：用你大学院研究里用过的样本（如 paper_all_company_folders 里的）？还是另外准备？
6. **时间预算**：你期望几周内完成？这会决定 phase 取舍的进度。

---

讨论好以上后，我会按你的方向更新这份方案的「实施细节」，并按 phase 推进，每完成一步都会 git commit + 同步到服务器。
